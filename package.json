{
  "name": "state-bank-of-india-app",
  "version": "1.0.0",
  "description": "Full-stack State Bank of India Application — monorepo with separate frontend and backend",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev": "start cmd /k \"cd frontend && npm run dev\" && start cmd /k \"cd backend && npm run dev\"",
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build"
  }
}
